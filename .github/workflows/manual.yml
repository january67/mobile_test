name: Approval check
on:
  pull_request_review:
    types: [submitted]
    branches:
     - 'releases'
 
jobs:
  approved:
    if: github.event.review.state == 'approved'
    runs-on: ubuntu-latest
    steps:
      - run: echo "This PR was approved"
